# Node modules
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# WhatsApp authentication session credentials (NEVER commit these)
auth_info_baileys/
*.session

# Environment variables
.env

# Runtime data & cache (Contains user messages - NEVER commit)
data/
*.jsonl

# OS Files
.DS_Store
Thumbs.db

# Large pre-bundled zip
mini-bot-with-modules.zip
